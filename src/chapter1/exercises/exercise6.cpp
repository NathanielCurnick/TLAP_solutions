#include <iostream>
#include <string>
using std::cin;
using std::cout;

int main() {
    cout << "Enter the number (in base 10) to convert to binary (base 2)";
    std::string input;
    cin >> input;
    std::string output;
    for (int i = 0; i < input.length(); i++){
    }
}